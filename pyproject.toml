[project]
name = "email-triage-env"
version = "1.0.0"
description = "Real-world email triage environment for RL agents"
requires-python = ">=3.8"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "httpx",
    "python-multipart",
    "openai"
]